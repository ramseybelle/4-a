def fall_distance(t):  # function
    g = 9.8
    d = (1 / 2) * g * t ** 2  # determining the distance
    return round(d, 3)  # return distance(round the value up to 3 decimal places as given in the question)
# t=float(input())
# dist=fall_distance(t)
# print(dist)
